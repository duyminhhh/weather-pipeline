# railway.toml — đặt ở ROOT repo

[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "node server.cjs"
restartPolicyType = "on_failure"

[nixpacks]
providers = ["node"]

[deploy.envVars]
NIXPACKS_APP_DIR = "weather-dashboard"